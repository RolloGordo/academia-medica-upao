// src/app/(auth)/layout.tsx
// Layout para todas las rutas de autenticación (login, etc.)

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}